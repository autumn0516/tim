# 快速开始指南

这是一个简单的5分钟快速开始指南，帮助你立即使用AI短视频生成工具。

## 🚀 一键启动（最简单）

```bash
# 1. 安装依赖（首次使用）
pip install -r requirements.txt

# 2. 启动Web应用
python run_web.py
```

然后在浏览器中打开 `http://localhost:5000` 即可使用！

## 📖 详细步骤

### 步骤1: 环境准备

#### 安装FFmpeg（必需）

**Ubuntu/Debian:**
```bash
sudo apt-get update && sudo apt-get install -y ffmpeg
```

**macOS:**
```bash
brew install ffmpeg
```

**Windows:**
从 [FFmpeg官网](https://ffmpeg.org/download.html) 下载并安装

#### 安装Python依赖

```bash
pip install -r requirements.txt
```

### 步骤2: 启动应用

**方式1: 使用启动脚本（推荐）**
```bash
python run_web.py
```

**方式2: 直接启动**
```bash
python -m web_app.app
```

### 步骤3: 使用Web界面

1. 在浏览器中打开 `http://localhost:5000`

2. **上传图片**
   - 点击或拖拽图片到上传区域
   - 可以上传多张图片
   - 支持 JPG, PNG, GIF 格式

3. **输入文本**（可选但推荐）
   - 输入视频的描述或想要添加的字幕
   - AI会根据文本分析视频风格
   - 例如：
     ```
     这是我们2024年夏天的海边旅行
     阳光、沙滩、美好的回忆
     ```

4. **上传背景音乐**（可选）
   - 支持 MP3, WAV, M4A 格式
   - 音乐会自动调整以匹配视频长度

5. **调整设置**（可选）
   - 点击"视频设置"展开选项
   - 可调整图片时长、转场效果、分辨率等

6. **生成视频**
   - 点击"AI生成视频"按钮
   - AI会自动分析内容
   - 等待几秒到几分钟（取决于图片数量）
   - 生成完成后可以预览、下载或分享

## 🤖 AI功能（可选）

如果你想使用AI智能分析功能，需要设置OpenAI API密钥：

```bash
export OPENAI_API_KEY="sk-your-api-key-here"
```

**AI功能包括:**
- 智能分析图片内容
- 理解文本含义和情感
- 自动推荐最佳视频风格
- 智能选择转场效果
- 自动生成字幕时间轴

**没有API密钥也可以使用！**应用会自动切换到基于规则的分析模式，同样能生成高质量视频。

## 📝 使用示例

### 示例1: 旅行相册视频

1. 上传5-10张旅行照片
2. 输入文本：
   ```
   2024年春天的日本之旅
   樱花盛开，美不胜收
   ```
3. 上传一首轻松的背景音乐
4. 点击生成

**结果**: 一个带淡入淡出效果、配有音乐和字幕的旅行视频

### 示例2: 产品展示视频

1. 上传3-5张产品图片
2. 输入文本：
   ```
   全新智能手表
   健康监测 | 长续航 | 时尚设计
   ```
3. 选择"专业"风格
4. 点击生成

**结果**: 一个专业的产品展示短视频

### 示例3: 生日祝福视频

1. 上传生日照片
2. 输入文本：
   ```
   生日快乐！
   愿你的每一天都充满欢笑和幸福
   ```
3. 上传欢快的生日歌
4. 点击生成

**结果**: 一个温馨的生日祝福视频

## 🎨 视频风格

AI会根据你的内容自动选择风格，或者你也可以通过关键词引导：

| 关键词 | 风格 | 特点 |
|--------|------|------|
| 温馨、家庭、回忆 | 温馨风格 | 柔和转场、暖色调 |
| 活力、运动、激情 | 活力风格 | 快速转场、明亮色调 |
| 专业、商务、企业 | 专业风格 | 简洁转场、中性色调 |
| 艺术、创意、设计 | 艺术风格 | 创意转场、艺术滤镜 |
| 复古、怀旧、经典 | 复古风格 | 复古滤镜、柔和转场 |

## 🎬 支持的视频格式

### 输出格式
- **横屏**: 1920x1080 (Full HD), 1280x720 (HD)
- **竖屏**: 720x1280 (抖音/快手)
- **方形**: 1080x1080 (Instagram)

### 转场效果
- **fade**: 淡入淡出（推荐，最常用）
- **slide**: 滑动
- **wipe**: 擦除
- **dissolve**: 溶解

### 视频特效
- 复古色调、黑白、棕褐色
- 模糊、锐化
- 亮度、对比度调整
- 晕影效果

## ⚙️ 高级配置

### 环境变量

```bash
# Flask配置
export FLASK_ENV=production        # 生产模式
export SECRET_KEY="your-secret"    # 安全密钥

# AI配置
export OPENAI_API_KEY="sk-xxx"     # OpenAI密钥
export OPENAI_MODEL="gpt-4"        # 使用的模型

# 上传限制
export MAX_CONTENT_LENGTH=104857600  # 100MB
```

### 自定义配置

编辑 `config.yaml` 文件可以自定义默认设置：

```yaml
video:
  default_resolution:
    width: 1920
    height: 1080
  default_fps: 30

audio:
  default_volume: 0.8

subtitle:
  font_size: 48
  font_color: "white"
```

## 🔧 故障排除

### 问题1: 启动失败

**解决方案:**
```bash
# 检查Python版本（需要3.8+）
python --version

# 重新安装依赖
pip install -r requirements.txt --upgrade
```

### 问题2: FFmpeg未找到

**解决方案:**
```bash
# 检查FFmpeg是否安装
ffmpeg -version

# 如果未安装，根据系统安装FFmpeg
```

### 问题3: 视频生成失败

**可能原因:**
- 图片格式不支持
- 磁盘空间不足
- 内存不足

**解决方案:**
- 使用JPG/PNG格式
- 清理磁盘空间
- 减少图片数量或尺寸

### 问题4: 上传失败

**解决方案:**
- 检查文件大小（默认限制100MB）
- 确认文件格式是否支持
- 检查uploads目录权限

## 📚 更多资源

- [完整文档](README.md)
- [Web应用详细说明](web_app/README.md)
- [API文档](README.md#api-文档)
- [示例代码](examples/)

## 💡 提示和技巧

1. **图片数量**: 3-10张图片效果最好
2. **图片质量**: 使用高质量图片会得到更好的视频
3. **文本描述**: 详细的文本有助于AI更好地理解内容
4. **音乐选择**: 选择与内容氛围匹配的音乐
5. **时长控制**: 每张图片3-5秒较为合适
6. **字幕**: AI会根据文本自动生成字幕建议

## 🎉 开始创作

现在你已经准备好了！启动应用，上传你的第一张图片，让AI帮你创作精彩的短视频吧！

```bash
python run_web.py
```

祝你创作愉快！🎬✨
